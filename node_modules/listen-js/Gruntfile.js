module.exports = function(grunt) {
    "use strict";

    grunt.initConfig({
        pkg: grunt.file.readJSON("package.json"),
        bt: {
            dist: 'dist',
            build: {
                files: {
                    'dist/listen.js': ['src/listen.js']
                },
                browserifyOptions: {
                    standalone: 'Listen'
                }
            },
            min: {
                files: {
                    'dist/listen-min.js': ['dist/listen.js']
                }
            },
            tests: {
                mocha: {
                    src: ['tests/*.js']
                }
            }
        }
    });

    require("load-grunt-tasks")(grunt);
};